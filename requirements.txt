# Core Flask
flask==3.1.3

# Database
Flask-SQLAlchemy==3.1.1
Flask-Cors==6.0.2
Flask-Migrate==4.1.0
Flask-SocketIO==5.6.0
alembic==1.18.3
psycopg2-binary==2.9.10

# Caching
redis==5.2.1

# Async task queue
celery[redis]>=5.3.0

# Environment variable management
python-dotenv==1.2.1

# Auth
bcrypt>=4.1.0
Flask-JWT-Extended>=4.6.0
Flask-Limiter>=3.5.0
Flask-Mail>=0.10.0
marshmallow>=3.21.0
email-validator>=2.0.0

# Email
resend>=2.0.0

# Testing
pytest==9.0.2
fakeredis>=2.0.0

# LLMs
semantic-kernel
openai
anthropic>=0.18.0
google-genai>=1.39.0
# Pinned to satisfy google-api-core>=2.30.0 while staying below proto-plus/googleapis upper bound
protobuf>=4.25.8,<7.0.0
aiohttp>=3.9.0
tokencost>=0.1.26

# OpenAI Agents framework
openai-agents
openai-agents[litellm]

# Training pipeline
torch>=2.5.1
torchvision>=0.20.1
torchaudio>=2.5.1
bitsandbytes>=0.46.0
transformers>=4.48.3
accelerate>=1.3.0
datasets>=3.2.0
peft>=0.14.0
trl>=0.14.0
evaluate>=0.4.0
scikit-learn>=1.4.0
matplotlib>=3.9.0
numpy
huggingface_hub
transformers[torch]
datasets[torch]
peft[torch]

# Optional: required for "Optimized" inference mode (GGUF via llama.cpp, fast on CPU).
# Run training/convert_to_gguf.py first to generate the .gguf file on HuggingFace.
#   pip install llama-cpp-python
# llama-cpp-python

# Optional: required for "Turbo" inference mode (Unsloth 4-bit quantisation, GPU only).
# Requires CUDA — install manually on supported hardware:
#   pip install unsloth
# unsloth
